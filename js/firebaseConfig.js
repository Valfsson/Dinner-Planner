const firebaseConfig = {
    apiKey: "***",
    authDomain: "***",
    databaseURL: "**",
    projectId: "dinner-planner-4540e",
    storageBucket: "**",
    messagingSenderId: "**",
    appId: "***",
    measurementId: "**"
  };

    // Initialize Firebase
    firebase.initializeApp(firebaseConfig);
   // firebase.analytics()
