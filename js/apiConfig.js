//Configuration files that contain keys like  js/apiConfig.js must never get into git repositories. 
//Create a .gitignore file in the project root folder (where vue.html and react.html sit). 
//The .gitignore file contains only one line:
const BASE_URL="*****";   // the DH2642 proxy server
const API_KEY="****";

